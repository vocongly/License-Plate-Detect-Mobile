name: license_plate_detect
description: A new Flutter project.
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  http: ^0.13.5
  flutter_spinkit: ^5.1.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  awesome_snackbar_content: ^0.0.8
  connectivity_plus: ^2.3.9
  internet_connection_checker: ^1.0.0+1
  image_picker: ^0.8.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^1.1.3
  build_runner: ^2.1.11

  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true
  assets:
    - assets/
    - assets/icons/
    - assets/images/
